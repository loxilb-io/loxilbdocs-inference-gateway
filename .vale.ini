StylesPath = .vale/styles
MinAlertLevel = error

# Prose linting runs as a non-blocking CI check. Expand BasedOnStyles or add
# packages (e.g. Google, write-good) as the team settles on a style guide.
[*.md]
BasedOnStyles = Vale
